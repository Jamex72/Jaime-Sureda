const mobileMenuButtons=document.querySelectorAll('.menu-toggle');const closeMobileMenu=button=>{const header=button.closest('.nav');header.classList.remove('menu-open');button.setAttribute('aria-expanded','false');button.setAttribute('aria-label','Abrir menú');document.body.classList.remove('menu-open')};mobileMenuButtons.forEach(button=>{const header=button.closest('.nav');const nav=header.querySelector('nav');button.addEventListener('click',()=>{const open=!header.classList.contains('menu-open');header.classList.toggle('menu-open',open);button.setAttribute('aria-expanded',String(open));button.setAttribute('aria-label',open?'Cerrar menú':'Abrir menú');document.body.classList.toggle('menu-open',open)});nav.addEventListener('click',event=>{if(event.target.closest('a'))closeMobileMenu(button)})});document.addEventListener('keydown',event=>{if(event.key==='Escape')mobileMenuButtons.forEach(closeMobileMenu)});addEventListener('resize',()=>{if(innerWidth>760)mobileMenuButtons.forEach(closeMobileMenu)},{passive:true});
